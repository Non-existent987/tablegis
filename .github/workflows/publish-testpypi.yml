name: Publish to TestPyPI (disabled)

# This workflow has been disabled from automatic triggers.
# It can be re-enabled manually if you need to run TestPyPI uploads.
on:
  workflow_dispatch: {}

jobs:
  disabled:
    runs-on: ubuntu-20.04
    steps:
      - name: Notice
        run: echo "This TestPyPI workflow is disabled for automatic runs. Use workflow_dispatch to run manually."